/*

Given an array a of size n, the Segment Tree must be able to find the sum of elements between the indices l and r i.e  a[l] + a[l+1] + a[l+2] + ..... + a[r], and also handle changing values of the elements in the array The Segment Tree should be able to process both queries in O(log n) time

*/

#include <bits/stdc++.h>
using namespace std;

int main()
{

    return 0;
}